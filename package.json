{
  "name": "jenkins-express-dummy",
  "version": "1.0.0",
  "description": "Dummy app for Jenkins study",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "mocha ./tests/test.js --reporter mocha-junit-reporter --reporter-options mochaFile=./test-results.xml"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "mocha": "^10.2.0",
    "mocha-junit-reporter": "^2.2.1"
  }
}
