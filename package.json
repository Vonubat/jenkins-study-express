{
  "name": "jenkins-express-dummy",
  "version": "1.0.0",
  "description": "Dummy app for Jenkins study",
  "main": "dist/index.js",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "mocha --require ts-node/register tests/test.ts --reporter mocha-junit-reporter --reporter-options mochaFile=./test-results.xml"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/mocha": "^10.0.6",
    "@types/node": "^24.0.0",
    "mocha": "^10.2.0",
    "mocha-junit-reporter": "^2.2.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
